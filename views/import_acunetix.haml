/* hadhemi */
.span10
  - if not @acunetixmap
    Acunetix Auto Mapping not enabled. Contact your administrator to enable.
  - else
    %form{:method => 'post', :action => "/report/#{@report.id}/import_autoadd", :enctype=>"multipart/form-data"}
      %br
      %h2 Auto Add Findings from acunetix scanner  (XML only)
      %br
      %table
        %tr
        %tr
          %td
            Upload acunetix scanner  file (XML only).&nbsp;
            %br
            %br
            %input{:type => 'file', :name => 'file'}
        %tr
          %td
            %br
      %br
      %br
      %input{:type => "hidden", :name => "type", :value => "acunetix"}
      %input{:type => 'submit', :value => 'Upload' }
      %button{:value => 'Cancel'} Cancel
